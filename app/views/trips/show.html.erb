<div id="logo" align="left">
  <img src="https://raw.githubusercontent.com/On-y-va/onyva_fe/main/src/assets/onyva_logo.png" class="img-fluid" alt="onyva logo" width="200" height="200" />
</div>
<center>
<div id="button" align="right">
  <div class='btn-group'>
    <%= button_to "Update Trip", edit_trip_path(@trip.id), method: :get, local: true, class:"btn btn-dark" %>&nbsp;&nbsp;&nbsp;
    <%= button_to "Flight Information", trip_flights_path(@trip.id), method: :get, local: true, class:"btn btn-dark" %>&nbsp;&nbsp;&nbsp;
    <%= button_to "Add Flight Information", new_trip_flight_path(@trip.id), method: :get, local: true, class:"btn btn-dark" %>&nbsp;&nbsp;&nbsp;
    <%= button_to "Delete #{@trip.name}", trip_path(@trip.id), method: :delete, local: true, class:"btn btn-danger" %><br/><br/><br/><br/>
  </div>
</div>

<h1 class="trip_name"> <%= @trip.name %> </h1>
image of city<br/>

Group Packing list:<br/><br/><br/><br/>
<div class="container text-center">
  <div class="row align-items-start">
    <div class="col">
      <h4>Places to eat:</h4>
      <ul>
      <% @trip_events.each do |trip_event| %>
        <li>Name: <%= trip_event.name %>
        Address: <%= trip_event.address %>
      <% end %>
      </ul>
    </div>
    <div class="col">
      Things to do:
    </div>
    <div class="col">
      Todays Itinerary:
    </div>
  </div>
</div>
</center>